export function initPortfolio() {
  // placeholder for modularizing existing inline scripts into section/scene modules
  // current page HTML already includes its own scripts, so this first pass just mounts structure
  console.log('Portfolio app initialized');
}
